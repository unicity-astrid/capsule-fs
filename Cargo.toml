[package]
name = "astrid-capsule-fs"
version = "0.1.0"
edition = "2024"
description = "Core filesystem tools for the Astrid microkernel"
publish = false

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
astrid-sdk = { version = "0.6.1", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"

# Local-path overrides while the per-domain WIT SDK is unpublished
# (sdk-rust feat/per-domain-wit). Will be removed once astrid-sdk 0.7.0
# ships to crates.io. See CAPSULE_MIGRATION_GUIDE.md.
[patch.crates-io]
astrid-sdk        = { path = "../../sdk-rust/astrid-sdk" }
astrid-sdk-macros = { path = "../../sdk-rust/astrid-sdk-macros" }
astrid-sys        = { path = "../../sdk-rust/astrid-sys" }
